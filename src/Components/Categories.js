import styles from './modules/Categories.module.css';

const Categories = () => (
  <>
    <h1 className={styles.container}>catagories</h1>
    <button className={styles.button} type="button">Check Status</button>
  </>
);

export default Categories;
